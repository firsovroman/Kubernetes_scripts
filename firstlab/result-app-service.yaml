apiVersion: v1
kind: Service
metadata:
  name: result-service
  labels:
    name : result-service
    app : demo-voting-app # совпадает с другими подами всего контура
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    pod : result-app-pod
    app : demo-voting-app # совпадает с другими подами всего контура